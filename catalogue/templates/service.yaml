apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    project: roboshop
    purpose: rsdemo
    tier: app
    microname: catalogue
spec:
  selector:
    project: roboshop
    purpose: rsdemo
    tier: app
    microname: catalogue
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP